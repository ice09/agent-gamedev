# Erster Auftrag für OpenCode

Füge den folgenden Auftrag in OpenCode ein:

```text
Lies AGENTS.md, GAME_SPEC.md und AGENT_HANDOFF.md vollständig.

Implementiere den gesamten ersten spielbaren Meilenstein direkt in diesem Repository. Verwende ausschließlich Godot 4.7.2, GDScript und einfache Godot-Primitiven; keine externen Assets oder Plugins.

Arbeite eigenständig: Lege benötigte Szenen, Skripte und Ressourcen selbst an. Halte die Architektur klein und verständlich. Verwende den cheap-worker nur für klar abgegrenzte mechanische Teilaufgaben und den reviewer für eine abschließende schreibgeschützte Kontrolle.

Lade nach der Implementierung den Skill godot-pruefen, führe alle Prüfungen aus und behebe gefundene Fehler. Lade anschließend den Skill uebergabe-pflegen und aktualisiere AGENT_HANDOFF.md.

Beende die Aufgabe erst, wenn das Projekt headless lädt, die Hauptszene kurz ohne offensichtliche Fehler läuft und die vollständige Spielschleife implementiert ist.
```

